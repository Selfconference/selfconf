= render partial: "shared/header", event: @event

.mid_block
  .container
    .row
      .col-md-12
        = react_component "Users",
              users: JSON.parse(@speakers.to_json(methods: [:selector?, :admin?]))
