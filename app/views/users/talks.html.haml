= render partial: "shared/header", event: @event

.mid_block
  .container
    .row
      .col-md-12
        %h4 Talks
        - @talks.each do |talk|
          = talk.name
          = talk.feedbacks.count
